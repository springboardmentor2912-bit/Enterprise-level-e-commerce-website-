# ============================================================
# Obsidian Luxury - Spring Boot Configuration
# Values are loaded from environment variables (.env) with local defaults
# ============================================================

# Server Configuration
server.port=${PORT:${SERVER_PORT:8080}}

# PostgreSQL Database Configuration
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/authdb}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:akash@973}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
app.jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
app.jwt.expiration=${JWT_EXPIRATION:86400000}

# CORS Configuration
spring.web.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173,https://*.vercel.app,https://e-commerce-two-phi-87.vercel.app,https://e-commerce-4mcy.vercel.app}

# Razorpay Integration (Test Mode)
razorpay.key_id=${RAZORPAY_KEY_ID:rzp_test_5Xv8eZ4Q9X0123}
razorpay.key_secret=${RAZORPAY_KEY_SECRET:DummySecretForTestMode12345}
razorpay.currency=${RAZORPAY_CURRENCY:INR}

# ShopStack Commission Settings
shopstack.commission.rate=${SHOPSTACK_COMMISSION_RATE:10.0}
